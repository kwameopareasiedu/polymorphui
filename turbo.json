{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["package/lib/dist/**"]
    },
    "dev": {
      "persistent": true,
      "cache": true
    }
  }
}
