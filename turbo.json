{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "outputs": ["packages/**/dist/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
